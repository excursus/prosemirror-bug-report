/* eslint-disable simple-import-sort/imports */
// Note: `init` must be run before `import React` to (if necessary) suppress react console prints.

import { attach } from './Text'


const root = document.getElementById("root")!
attach('prosemirror-root',  true)

